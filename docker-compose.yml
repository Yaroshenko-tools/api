# Local development only. On Coolify, use the Dockerfile build pack (see DEPLOY.md).
version: "3.8"

services:
  backend:
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - .env
